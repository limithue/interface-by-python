\# 面向AI的端口扫描工具开发指令任务书（正式版）

\## 一、任务基本信息

\- 任务名称：Windows 轻量级多线程端口扫描工具开发与实现

\- 执行对象：AI 编程助手

\- 依据资料：config.py、main.py、python接口.py 完整源码

\- 任务性质：代码解析、功能实现、逻辑优化、文档输出

\- 使用场景：授权内网安全巡检、端口开放检测、运维测试



\## 二、核心任务目标

1\. 基于给定三套代码，实现可直接运行的稳定端口扫描工具。

2\. 完整解析代码结构、执行流程、函数作用与模块调用关系。

3\. 保证多线程安全、IP/端口解析正确、输出结果清晰。

4\. 提供可直接使用的教程、运行说明与风险提示。

5\. 遵循合规要求，仅用于授权内网测试。



\## 三、AI 需完成的具体任务

\### （一）代码解析任务

1\. 逐文件解读功能：config、main、python接口各自职责。

2\. 解释核心逻辑：socket 扫描、多线程、线程锁、IP 存活检测、端口判断机制。

3\. 说明执行流程：输入→解析→存活检测→多线程扫描→结果输出。

4\. 标注关键函数：scan\_tcp、is\_alive、PortScanner、\_worker、start\_scan。



\### （二）功能实现任务

1\. 整合代码，确保可直接运行，无导入错误。

2\. 支持：域名/IP 解析、common 端口/单端口/端口段解析。

3\. 实现 TCP 端口开放检测、IP 存活预检、多线程并发。

4\. 自动匹配端口对应服务名称并友好展示结果。

5\. 处理异常：解析失败、端口格式错误、无存活 IP 等。



\### （三）优化与增强任务

1\. 优化线程调度，避免崩溃，保证数据安全无冲突。

2\. 兼容 Windows 普通权限，降低管理员权限依赖。

3\. 增加隐蔽性：随机延迟，避免被防火墙拦截。

4\. 增强鲁棒性：输入校验、超时控制、错误提示。



\### （四）文档输出任务

1\. 输出完整任务框架、模块说明、功能清单。

2\. 输出正式任务计划书、工期计划、交付物清单。

3\. 输出使用教程、运行步骤、常见问题解决。

4\. 输出合规声明、风险提示、使用限制。



\## 四、交付成果清单

1\. 可直接运行完整源码（3 个文件）

2\. 代码结构与流程图说明

3\. 任务框架文档

4\. 正式任务计划书

5\. 工具使用教程

6\. 测试说明与风险提示

7\. 合规使用声明



\## 五、技术要求与约束

1\. 语言：Python3，无第三方库依赖，仅用内置模块。

2\. 协议：TCP 扫描为主，ICMP 存活检测，UDP 可选。

3\. 线程：最大 20 线程，分批启动，带线程锁安全写入。

4\. 权限：Windows 需提示管理员权限，无权限可降级运行。

5\. 合规：严禁用于未授权网络，仅限内网授权使用。



\## 六、质量标准

1\. 扫描结果准确，低误报、低漏报。

2\. 多线程运行稳定，不闪退、不冲突。

3\. 交互友好，提示清晰，报错明确。

4\. 代码规范、注释完整、结构清晰。

5\. 文档格式正式、可直接提交使用。



\## 七、风险与应对要求

1\. 无管理员权限：跳过 ICMP 存活检测，继续扫描。

2\. 防火墙拦截：降低线程、增加随机延迟，提升隐蔽性。

3\. IP 无法解析：给出明确提示，退出流程。

4\. 扫描过快被限制：控制并发，固定最大线程数。



\## 八、AI 执行规范

1\. 严格依据提供的三份代码实现，不随意增删功能。

2\. 保持工具轻量化、命令行运行，不开发 GUI。

3\. 不实现漏洞利用、暴力破解、攻击功能。

4\. 所有输出内容必须正式、清晰、可直接用于作业/项目交付。

