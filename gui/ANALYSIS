PyQt6 端口扫描工具 - 代码问题分析
1. 致命问题（崩溃 / 无法使用）
1.线程暂停 / 恢复 / 停止功能失效
ThreadManager 的控制仅作用于任务队列，无法真正阻塞或退出 QThread，后台扫描继续运行，界面为假控制。
2.队列读取不安全
使用 result_queue.get_nowait() 无异常捕获，队列为空时直接触发 Empty 异常导致程序崩溃。
3.PortScanner 返回类型不一致
代码中同时用元组判断 res[0] == 'error' 与字典取值 res['ip']，运行时必定触发类型错误。
4.关闭窗口线程不退出
关闭 GUI 后后台扫描线程继续运行，造成进程残留，无法完全退出。
5.UDP 扫描未实现但界面可选择
仅支持 TCP 扫描，UDP 无实际逻辑，选择 UDP 会导致结果错误、无检测意义。

2. 功能性缺陷（失效 / 异常）
6.暂停按钮无法切换恢复
仅绑定暂停函数，无恢复逻辑，暂停后无法继续扫描。
7.导出功能格式固定
export_data() 硬编码为 TXT 格式，不支持 CSV 选择，与按钮文字不符。
8.进度条数值越界
进度值无上限保护，可能超过 100%；任务数为 0 时无自动重置。
9.异常捕获不完整
Ping 异常、Socket 错误、IP 解析失败会直接崩线程，无错误提示。
10.重复启动线程风险
停止后未清空线程对象，多次点击开始会并发多轮扫描，导致混乱。

3. 并发与安全缺陷
11.结果无线程安全保护
多线程写入结果列表未加锁，可能出现结果丢失、重复、错乱。
12.轮询循环无法即时响应停止
run() 内 while + sleep 轮询，无法立即停止，退出延迟高。
13.无扫描速度控制
发包过快，易被防火墙拦截，无随机延迟与限流机制。

4. UI 与体验缺陷
14.“跳过离线 IP” 控件使用错误
用 QPushButton 做开关，视觉误导用户，应改为 QCheckBox。
15.日志可读性差
无时间戳，OPEN/WARN/ERROR 无颜色区分，排查困难。
16.按钮状态管理混乱
开始 / 暂停 / 停止 / 导出状态不同步，易误操作。

5. 工程化规范问题
17.无窗口关闭保护
未重写 closeEvent，直接关闭可能导致任务异常中断。
18.函数内导入模块不规范
run() 内 import time，影响性能且不符合编码规范。
19.无任务总量上限
超大网段 + 端口段会瞬间占满内存，导致程序卡死。
20.无合规使用提示
缺少授权检测与法律声明，不符合安全工具开源规范。
